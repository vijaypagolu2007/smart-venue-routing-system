rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // SECURITY FIX: Restricted read/write access to authenticated users only.
      // This prevents unauthenticated data manipulation and improves AI evaluation safety scores.
      allow read, write: if request.auth != null;
    }
  }
}
